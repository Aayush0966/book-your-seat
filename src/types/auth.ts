export interface SignupDetails {
    fullName: string;
    contactNumber: number;
    email: string;
    password: string;
}

export interface CredentialsType {
    email: string;
    password: string;
}